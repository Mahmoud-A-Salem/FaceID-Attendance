@using NewFaceIDAttendance.ViewModels
@model AssignCourseToStudentVM
@{
    Layout = "_AdminLayout";
}

<h2>Assign Courses to @Model.StudentName</h2>

<form method="post">

    @Html.HiddenFor(m => m.StudentID)

    <div class="mb-3">
        <label class="form-label">Courses</label>
        @Html.ListBoxFor(m => m.SelectedCourses, Model.Courses, new { @class = "form-control", size = "10" })
    </div>

    <button class="btn btn-primary">Save</button>
</form>
